# Material del curso

Material del módulo propagado a este repo (refrescable con
`bash scripts/bootstrap-docs-curso.sh`; el refresh copia encima, no borra).

Cada fase añade su archivo `clase-N-{slug}.md` con un resumen de qué se
construyó, las decisiones tomadas y los descubrimientos de esa clase.
